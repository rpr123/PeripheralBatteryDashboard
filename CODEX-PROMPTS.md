# Codex 작업 프롬프트

이 문서는 Peripheral Battery Dashboard의 설치·업데이트·제거와 기기별 조정을 로컬 Codex 또는 다른 코딩 에이전트에 맡길 때 전달하는 프롬프트 모음입니다. 사용자가 저장소 복제, ZIP 해제, 빌드와 설치 파일 배치를 직접 수행하는 흐름은 전제하지 않습니다.

공식 저장소: https://github.com/rpr123/PeripheralBatteryDashboard

## 사용 방법

1. 설치할 Windows PC에서 로컬 셸과 파일을 사용할 수 있는 Codex 또는 코딩 에이전트를 엽니다.
2. 아래에서 설치·진단·기기 추가·제거 중 목적에 맞는 프롬프트 하나를 골라 **코드 블록 전체를 복사해 전달**합니다. 저장소는 에이전트가 승인 후 확보하므로 사용자가 먼저 내려받을 필요가 없습니다.
3. 괄호로 표시한 기기 정보는 알고 있을 때만 실제 값으로 바꿉니다. 기기 목록에는 한 줄에 하나씩 여러 제품을 적을 수 있으며 기기마다 프롬프트를 따로 보낼 필요가 없습니다. 1번 설치 프롬프트에서는 목록을 모두 비워도 에이전트가 승인된 읽기 전용 descriptor 인벤토리로 연결된 HID 후보를 찾는 것이 기본 흐름입니다.
4. 에이전트가 설명하는 다운로드·설치·저장소 밖 쓰기·레지스트리/자동 실행 변경·실기기 I/O의 대상과 영향을 확인하고 필요한 단계만 승인합니다.

LLM이 웹 채팅만 할 수 있고 로컬 파일·Windows 장치에 접근할 수 없다면 실제 설치나 USB 동글 확인은 수행할 수 없습니다. 이 경우 에이전트가 제시하는 명령을 무조건 실행하지 말고, 로컬 접근이 가능한 코딩 환경에서 다시 진행하세요.

경량 모델은 설치·업데이트·읽기 전용 인벤토리, 표준 BLE Battery Service `180F/2A19` 등록, 이미 검증된 공급자와 동일한 프로토콜의 좁은 JSON 프로필 작성 같은 정형 작업에 사용할 수 있습니다. **프로토콜이 알려지지 않은 USB 동글/HID 기기의 식별 충돌 해소, 공개 근거 분석, 프로토콜 추출, 새 공급자 구현과 독립 검토에는 frontier급 고수준 추론 에이전트를 사용하세요.** 현재 기준 권고는 GPT-5.6 Sol `high`/`xhigh`이며 특히 어려운 경우 `max`를 검토합니다. Luna급 모델은 신규 동글 프로토콜 작업의 단독 구현자나 단독 검토자로 적합하다고 전제하지 않습니다. 다만 장치·근거마다 난도가 달라 성공을 보장하는 정확한 최소 모델이나 추론 수준은 정할 수 없으므로, 모델 이름이 아니라 근거 대조·fixture·빌드·self-test·실기기 검증·독립 검토 통과로 완료를 판정합니다. 경량 모델은 조사를 생략해 즉시 `지원 보류(blocked)`로 끝내거나 불완전한 구현을 설치하지 말고, 안전한 인벤토리와 조사 기록을 고수준 에이전트에 인계해야 합니다. 어떤 모델도 근거 없는 HID 명령을 시험할 권한은 없습니다.

## 1. 처음 설치하고 호환성 확인

```text
Peripheral Battery Dashboard를 내 Windows PC에 안전하게 설치하고 아래 목록에 적은 모든 주변기기의 호환성을 한 작업에서 확인해 줘. 공개 배포본에는 활성 기본 기기 프로필이 하나도 없으므로, 각 PC에서 실제로 확인한 장치만 네가 배터리 프로토콜을 직접 조사·검증하고 사용자 프로필 및 필요한 공급자로 등록해야 한다. 내가 저장소를 직접 복제하거나 ZIP을 내려받아 압축 해제하고, 소스를 빌드하거나 설치 파일을 배치하도록 넘기지 말고 네가 승인 경계를 지키며 전체 설치를 수행해라. 기기 목록이 비어 있어도 누락을 이유로 멈추지 말고, 이 요청 하나 안에서 설치 → self-test → 승인된 읽기 전용 HID 및 표준 Bluetooth Battery Service 인벤토리 → 표준 BLE 범용 프로필 등록 또는 후보별 프로토콜 조사 → 근거가 충분한 공급자 구현·등록과 fixture 검증까지 계속하라. 목록의 기기와 새로 발견한 후보를 각각 추적하고, 한 기기의 실패나 지원 보류 때문에 나머지를 생략하거나 별도 요청으로 넘기지 마라. XInput-only 또는 audio-only처럼 HID collection과 표준 Battery Service에 나타나지 않는 장치는 자동 발견을 보장한다고 말하지 말고 7단계의 최소 질문과 안전한 Windows 메타데이터 확인으로 이어가라.

대상 저장소: https://github.com/rpr123/PeripheralBatteryDashboard

모델 적합성: 표준 Bluetooth Battery Service 등록이나 기존 공급자와 동일한 프로토콜의 JSON 프로필 작성은 경량 모델로도 진행할 수 있다. 프로토콜이 알려지지 않았거나 기존 공급자와 다른 USB 동글/HID 기기의 조사·프로토콜 추출·새 공급자 구현은 frontier급 고수준 추론 에이전트가 필요한 작업이며, 현재 기준으로 GPT-5.6 Sol high/xhigh(특히 어려우면 max)를 권고한다. Luna급 모델은 이 작업의 단독 구현자나 단독 검토자가 되어서는 안 된다. 다만 성공을 보장하는 정확한 최소 모델·추론 수준은 장치별로 달라 정할 수 없으므로, 원본 근거·fixture·빌드·self-test·실기기 검증·독립 검토를 완료 기준으로 삼는다. 현재 에이전트가 적합하지 않으면 안전한 인벤토리와 조사 기록을 보존해 고수준 에이전트에 인계하고 불완전한 구현을 설치하거나 지원 완료를 주장하지 않는다.

절대 종료 규칙: `저장소에 프로토콜이 없다`, `기본 목록에 없다`, `사용자가 프로토콜 문서·캡처·명령 바이트를 주지 않았다`는 말만으로 지원 보류하거나 작업을 끝내지 마라. 프로토콜 발견은 이 요청의 핵심이며 에이전트가 수행할 일이다. 먼저 외부로 전달할 redacted 식별 정보와 조사 범위를 설명하고 네트워크 접근 승인을 요청하라. 승인되면 제조사 공식 문서·지원/다운로드 페이지·법적으로 검토 가능한 웹 드라이버 자산·감사 가능한 기기별 오픈소스와 공개 raw source를 네가 직접 조사해 읽기 전용 배터리 요청·응답 근거를 찾아라. 네트워크 권한이 없으면 사용자에게 프로토콜을 찾아 달라고 요구하지 말고 필요한 권한 승인을 요청하라. 문서의 검색 행렬과 출처 종류를 실제로 확인하고 구체적인 URL·질의·결과를 보고한 뒤에도 근거가 없을 때만 해당 기기를 `지원 보류(blocked)`로 판정할 수 있다. 검색 없이 즉시 보류한 결과는 작업 실패다.

1. 먼저 읽기 전용으로 Windows 10/11 x64 여부, .NET Framework 4.8, Git과 VS 2022 Build Tools 유무, 기존 설치 위치와 후보 설치 경로를 확인하고 계획을 요약한다. 앱 실행과 현재 사용자용 패키지 설치에는 관리자 권한이 필요하지 않다. 누락된 Microsoft 런타임이나 빌드 도구가 권한 상승을 요구하면 별도 승인 없이 설치하지 말고 검증된 패키지 경로 또는 `설치 보류(blocked)`를 선택한다.
2. 기존 프로젝트 폴더가 있으면 origin URL이 위 공식 URL과 같은지, HEAD와 dirty 상태를 읽기 전용으로 확인하고 사용자 변경을 보존한다. origin이 다르면 그 복사본을 실행하지 않는다. 기존 로컬 저장소의 존재 여부와 관계없이 기본 설치 입력은 공식 저장소의 `v1.1.3` GitHub Release다. 로컬 저장소는 상태만 확인하고, 소스 수정이나 빌드가 필요한 경우에만 같은 공식 `v1.1.3` 태그를 별도 승인 후 사용한다. 기본 설치에는 Release의 `PeripheralBatteryDashboard-v1.1.3-win-x64.zip`과 `SHA256SUMS.txt`를 사용하며, 정확한 태그·자산 URL·대상 경로·생길 파일을 보여 준 뒤 네트워크와 쓰기에 대한 승인을 기다린다. Release는 draft·prerelease가 아닌지, 저장소 소유자와 이름·태그·자산 이름이 정확한지 확인하고 제3자 미러, GitHub Actions artifact, 저장소 트리의 ZIP 또는 다른 버전의 문서·해시를 섞지 않는다.
3. Release 패키지를 선택하면 같은 공식 `v1.1.3` Release의 ZIP과 `SHA256SUMS.txt`를 받고, 압축 해제나 실행 전에 SHA-256을 정확히 비교한다. 불일치하거나 같은 파일명의 항목이 중복되면 즉시 중단한다. 일치하면 압축을 해제한 뒤 README.md에서 공식 배포 경로와 사용자에게 약속된 동작을 확인하고, AGENTS.md의 공식 배포 선택과 강제 안전·승인·개인정보·구현·검증·게시 규칙을 따른다. 새 기기 조사·구현에는 DEVICE-ADDING.md의 절차를 적용하고, CODEX-PROMPTS.md는 목적별 작업 요청의 참고로 사용한다. 소스 경로를 승인받았다면 공식 origin과 `v1.1.3` 태그가 가리키는 commit을 확인하고 그 태그의 문서·소스만 사용한다. Release를 사용할 수 없으면 임의의 이전 배포본으로 낮추지 말고 공식 태그의 소스 빌드를 별도로 승인받으며, SmartScreen이나 보안 경고를 임의로 우회하지 않는다.
4. 검증된 배포본과 소스 빌드 중 적합한 방법을 선택한다. 빌드한다면 격리된 출력 폴더를 사용한다. 다운로드, 빌드 도구·런타임 설치, 저장소 밖의 안정된 앱 폴더 쓰기 등 외부 상태 변경 전에 정확한 대상과 영향을 설명하고 내 승인을 받는다.
5. 실행 파일을 호출하기 전에 Windows `FileVersionInfo` 메타데이터를 읽어 현재 배포 기준인 1.1.3 이상인지 확인한다. 그보다 오래된 버전에서는 이 문서가 요구하는 인벤토리·조회 격리·복구 동작을 전부 보장할 수 없으므로 새 옵션이나 실기기 진단을 시험하지 말고 1.1.3 이상 배포본 또는 소스 빌드로 교체한다. 버전을 확인한 뒤에만 `--version`과 `--help`를 실행해 `--inventory` 지원과 알 수 없는 옵션의 비대화형 오류 종료를 확인한다. 그다음 실제 장치 I/O가 없는 `PeripheralBatteryDashboard.Diagnostics.exe --self-test`를 실행하고 종료 코드 0을 확인한다. 빌드 또는 self-test가 실행되지 않았거나 실패했다면 구현 완료로 보고하지 않는다.
   Windows PowerShell 실행 정책이 스크립트를 막으면 `-ExecutionPolicy Bypass`를 추가하거나 시스템 정책을 바꾸지 않는다. 이미 설치된 신뢰 가능한 `pwsh`로 같은 저장소 스크립트를 실행하고, 그것도 없으면 필요한 설치와 영향을 설명해 승인받기 전까지 빌드를 보류한다.
6. 기기 목록이나 제품명이 비어 있어도 `PeripheralBatteryDashboard.Diagnostics.exe --inventory`로 Windows에 HID collection으로 노출된 USB 동글·유선·Bluetooth HID 후보와, Windows에 등록된 표준 Bluetooth Battery Service 인터페이스를 자동 탐색한다. 실행 전에 정확한 실행 파일과 열거 대상, descriptor·서비스 인터페이스 메타데이터 조회이며 provider/battery 요청, HID input read·output write·Feature I/O와 GATT characteristic 값 읽기를 전혀 수행하지 않는다는 점, redacted 출력 필드를 설명하고 내 승인을 기다린다. 승인 후 실행하며 종료 코드 0, `complete=true`, `profileWarningCount=0`인지 먼저 확인한다. 하나라도 아니면 빈 목록을 `기기 없음`으로 해석하지 말고 `warningCodes`와 `coverage`를 보고한 뒤 불완전 상태로 둔다. 정상 결과에서는 `researchCandidate=true`인 그룹과 broad selector를 미지원·재검토 후보로 삼는다. `exact-selector-match`는 프로필 선택자 일치일 뿐 공급자 로드나 실제 배터리 조회 성공의 증명이 아니므로 self-test와 공급자 등록도 확인한다. `bestEffortSanitizedProductString`은 일부 민감 패턴을 가렸을 뿐 개인정보 비포함을 보장하지 않으므로 외부 검색어로 보내기 전에 로컬 검토하고, `deviceGroupId`도 로컬 비교용 가명 값이므로 외부 검색에 쓰지 않는다. 이어서 기기 목록이 비어 있는 기본 흐름에서는 XInput/game-controller, 표준 Battery Service가 없는 Bluetooth, 오디오 엔드포인트와 현재 연결된 PnP 장치의 읽기 전용·redacted Windows 메타데이터 점검도 대상·필드·개인정보 제거 방식을 설명하고 승인받아 수행한다. 장치 요청이나 공급자 실행을 해서는 안 된다. 그래도 XInput-only·audio-only·표준 Battery Service가 없는 Bluetooth 장치 존재를 확정할 수 없을 때만 배터리 주변기기가 더 있는지 한 번의 통합 최소 질문을 한다. 설치가 끝났다는 이유로 여기서 작업을 종료하지 말고 발견한 미지원 후보 전부에 대해 7~8단계를 같은 요청 안에서 계속 수행한다.
   같은 `--inventory`는 GATT characteristic 값을 읽지 않고 Windows에 등록된 표준 Bluetooth Battery Service `180F` 인터페이스 메타데이터도 열거한다. `bluetoothBatteryServices` 항목이 있으면 `2A19` 표준 잔량 프로토콜이 이미 확인된 것이므로 제조사별 프로토콜 탐색보다 `builtin.bluetooth.gatt-battery` 사용자 프로필 등록을 우선한다. 해당 항목의 이 PC 전용 `localServiceId`를 항상 사용하고, exact VID/PID가 있으면 추가 AND 조건으로 함께 기록한다. `localServiceId`는 가명 로컬 식별자이므로 외부 검색·공개 프로필·이슈에 넣지 않는다. 같은 조건의 후보가 여러 개면 첫 항목을 임의 선택하지 말고 검토한 이름 보조 조건으로 더 좁힌다. 표준 Battery Service가 없는 Bluetooth 기기만 7단계의 제조사 전용 프로토콜 조사로 넘긴다.
7. 발견한 미지원 후보가 있으면 사용자가 기기별 추가 프롬프트나 프로토콜 자료를 주기를 기다리거나, `BT5.3 MOUSE CH1`, `2.4G/USB Receiver` 같은 generic 이름 또는 기본 목록에 없다는 이유만으로 즉시 `지원 보류(blocked)`로 끝내지 마라. 로컬 검토를 마친 제품 문자열과 VID/PID가 명확하면 바로 기기별 조사를 시작한다. generic receiver가 모호하면 해당 기기만 켠/끈 상태 또는 해당 동글만 연결/분리한 상태에서 `--inventory`를 다시 실행하는 differential inventory의 대상·출력·사용자 동작을 설명하고 승인을 받아 에이전트가 두 redacted 결과의 안정적인 `deviceGroupId`와 HID identity tuple을 비교한다. 그래도 식별되지 않을 때만 정확한 제품명이나 시리얼·바코드를 가린 라벨 사진을 최소 질문으로 한 번 요청하고, 답을 받아 같은 작업을 계속한다. XInput-only·audio-only처럼 inventory에 보이지 않는다고 알려진 기기는 안전한 Windows 메타데이터를 먼저 확인하고, 식별에 꼭 필요한 정확한 제품명만 최소 질문으로 요청한다. 완전 유선이거나 배터리가 없는 장치는 제조사 사양 또는 Windows 메타데이터 근거가 있을 때만 배터리 비대상으로 제외한다. 네트워크 조사 전에 검색 범위와 외부 전송 정보를 설명하고 내 승인을 받은 뒤 미지원 후보를 기기별로 나눠 가능한 범위에서 병렬로 제조사 공식 문서와 지원/다운로드 페이지, 법적으로 검토 가능한 제조사 웹 드라이버 자산, 감사 가능한 기기별 오픈소스 구현 순으로 직접 조사한다. URL, 정확한 버전·커밋 또는 확인 날짜, 대상 리비전과 라이선스를 기록하고 출처 불명 또는 라이선스가 맞지 않는 코드는 복사하지 않는다.
   근거가 없다고 결론내리기 전에 exact VID:PID의 콜론 표기와 `VID_xxxx&PID_xxxx` 표기, 제품 문자열, MI·Usage·보고서 길이, 캡처의 각 고정 바이트 prefix, 패킷 내부 device/model ID의 16진수·10진수 조합으로 검색 행렬을 만들고 문서뿐 아니라 공개 raw source/code도 검색한다. 첫 VID/PID 결과나 소매 모델 저장소 하나만 보고 끝내지 않는다. 지원 보류 시 실제 질의 변형과 확인한 출처 종류를 보고한다.
8. 기존 읽기 전용 프로토콜과의 동일성이 확인되면 좁게 매칭한 완전한 사용자 JSON 프로필과 fixture 테스트를 만들고, 요청·응답 형식이 다르면 별도 `IBatteryProvider` 플러그인·사용자 프로필·모의 응답 fixture를 구현한다. `Profiles/builtin.devices.json`은 의도적으로 빈 배포 계약이므로 개인 기기를 넣지 않는다. VID/PID가 여러 소매 모델에서 공유되거나 캡처에 별도 device/model ID가 있으면 그 내부 ID까지 복수 출처와 대조하고, 충돌이 남으면 임의 모델명 대신 검증된 제품군 이름을 쓰거나 보류한다. 기존 공급자를 재사용할 때도 먼저 공개 근거로 정확히 같은 프로토콜임을 입증하고 `ReadAsync`의 장치 발견·연결 판정·identity binding 전체를 추적하며, 모든 XInput 슬롯을 무차별 순회하는 공급자를 여러 프로필에 붙여 중복 표시하지 않는다. 송신 없는 수동 입력 프로토콜은 `HidSession.OpenReadOnly`와 input read만 사용한다. 알 수 없는 HID interrupt/output/Feature write, 무작위 바이트, 명령 공간 스캔·퍼징, 다른 모델 명령의 추측 재사용, 펌웨어·페어링·DPI·키맵·온보드 설정 변경은 절대 사용하지 않는다. 공개 자료를 실제로 조사했지만 exact hardware identity나 읽기 전용 배터리 프로토콜 근거가 여전히 부족한 경우에만 검색한 출처와 부족한 증거를 적어 `지원 보류(blocked)`로 보고한다.
   제공되거나 승인된 캡처와 문서에 서로 다른 상태/subtype이 있으면 모든 표본을 이름 있는 회귀 fixture로 만들고 충전·방전·유선·절전·사용 불가 상태를 각각 올바르게 매핑한다. 일부 표본만 처리하고 나머지를 조용히 버리면 완료로 보고하지 않는다. 알 수 없는 subtype은 거부하되, 근거가 고정값이라고 입증하지 않은 패딩·후행 바이트까지 0으로 강제하지 않는다.
   새 프로토콜 또는 새 공급자는 자동 적용 전에 구현하지 않은 별도의 frontier급 고수준 추론 에이전트가 독립 검토를 한 번 더 수행한다. 검토자는 원본 근거·URL·모든 캡처·parser fixture·전체 `ReadAsync` 경로를 다시 열어 상태/subtype 의미, identity binding, 핸들 접근 권한과 금지된 쓰기 호출을 확인한다. 적합한 독립 검토를 수행할 수 없거나 불일치가 남으면 프로필/플러그인을 설치·로드하지 않고 `미검증/부분 지원`으로 보고한다.
9. `--diagnostics`와 `--snapshot`은 매칭된 ProviderId의 검증된 배터리 요청을 실제 장치에 보낼 수 있다. 새 명령을 포함해 실행 전에 정확한 ProviderId·VID/PID·MI·Usage·보고서 타입과 길이·전송 바이트·읽기 전용 근거·예상 응답·실패 영향을 보여 주고 내 별도 승인을 기다린다. 승인 전에는 실기기 요청을 보내지 않는다. 장치 시리얼, 전체 장치 경로, Bluetooth 주소, 사용자명, 토큰은 출력·업로드·커밋하지 않는다.
10. 구현과 비실기기 검증이 끝난 뒤 GUI 첫 실행 전에 자동 실행을 켤지 끌지 묻는다. 끄기를 선택하면 전체 설정 파일과 `StartWithWindows=false` 쓰기를 설명하고 승인 후 먼저 준비한다. 켜기를 선택하면 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\PeripheralBatteryDashboard` 값과 정확한 실행 명령을 보여 준다. GUI는 이 레지스트리 상태를 동기화하고 모니터를 시작해 exact-match 공급자의 장치 요청을 보낼 수 있음을 함께 설명한 뒤 첫 실행에 대한 별도 승인을 기다린다. 승인이 없으면 GUI 실행과 관련 파일·레지스트리 변경을 보류한다.
11. 최종 보고에는 공식 소스의 origin·tag·commit 또는 공식 GitHub Release의 태그·자산 URL·검증한 SHA-256, 설치 위치, 관리자 권한 사용 여부, 변경 파일, 조사한 URL·리비전·라이선스, fixture·빌드와 --self-test의 실제 실행 명령과 종료 코드, 자동 실행 선택·승인과 실제 파일/레지스트리 상태, 실기기 I/O 여부와 ProviderId, 새 HID write 전송 여부를 포함한다. 컴파일이나 테스트가 실행되지 않았거나 실패했다면 지원 완료가 아니라 미검증/실패로 판정한다. `--inventory`와 승인된 Windows 메타데이터 점검에서 확인했거나 최소 질문으로 식별한 후보마다 `내장 공급자 재사용+사용자 프로필 등록 / 새 공급자 구현+사용자 프로필 등록 / 배터리 비대상 / 조사 후 지원 보류` 중 하나의 결론과 근거를 빠짐없이 적고, generic 이름만 반복하거나 `지원 판정 보류`로 남겨 두지 않는다. 실제 등록한 사용자 프로필과 공급자가 없으면 설치만으로 호환성 작업을 완료했다고 보고하지 않는다. 내 요청 없이 GitHub 게시, 커밋, 푸시 또는 릴리스를 하지 않는다.

사용 제품 및 실제 연결 방식(한 줄에 하나씩, 여러 개 가능, 전부 비워도 됨):
- [제품명/리비전] — [Bluetooth / 2.4GHz USB 동글 / 유선]
- [제품명/리비전] — [Bluetooth / 2.4GHz USB 동글 / 유선]
- ...
```

## 2. 변경 없이 진단만

```text
이 저장소와 Peripheral Battery Dashboard를 사용해 아래 목록의 기기들이 표시되지 않거나 배터리 값이 잘못되는 원인을 각각 진단해 줘. 여러 기기가 있으면 모두 같은 작업에서 다루고, 한 기기의 진단 실패 때문에 나머지를 생략하지 마라. 이번 요청은 진단만이며 소스, 사용자 프로필, 레지스트리, 자동 실행, 드라이버와 설치 상태를 변경하지 마라.

대상 저장소: https://github.com/rpr123/PeripheralBatteryDashboard

기존 설치 폴더와 포함 문서 또는 위 공식 origin의 로컬 저장소가 없으면 이번 진단 요청에서는 다운로드·복제하지 말고 `진단 보류(blocked)`로 끝낸 뒤 1번 설치 프롬프트를 안내한다.

먼저 README.md, AGENTS.md, CODEX-PROMPTS.md, DEVICE-ADDING.md를 읽어라. Git 상태와 앱 버전을 확인하고 장치 I/O가 없는 --self-test를 먼저 실행한다. 이어서 대상·redacted 필드와 승인 경계를 설명하고, 승인 후 provider/battery 요청, HID input read·output write·Feature I/O와 GATT characteristic 값 읽기를 전혀 수행하지 않는 redacted `--inventory`로 Windows HID collection descriptor와 등록된 표준 Bluetooth Battery Service 인터페이스 메타데이터를 확인한다. XInput-only·audio-only·표준 Battery Service가 없는 Bluetooth 장치의 자동 발견은 보장하지 않는다. 이번 진단 요청에서는 GUI를 실행하거나 재시작하지 않는다. --diagnostics와 --snapshot은 매칭된 기존 공급자의 검증된 요청을 실제 장치에 보낼 수 있으므로 수동 파일 검사처럼 표현하지 말고, 실행 전에 대상 ProviderId·장치 매칭·수행될 I/O를 설명한 뒤 내 명시적 확인을 기다린다. 확인을 받았고 필요한 경우에만 사용한다. 제조사 프로그램의 장치 독점, 절전, 연결 방식, VID/PID, 인터페이스와 Usage, 선택된 ProviderId를 점검하되 장치 시리얼과 전체 경로, 사용자명, Bluetooth 주소 같은 식별 정보는 결과에 노출하지 마라.

`--inventory`의 `bluetoothBatteryServices`도 확인한다. 여기에는 characteristic 값 읽기 없이 확인한 표준 180F 서비스만 나오며, 해당 항목이 있으면 `builtin.bluetooth.gatt-battery` 범용 경로를 먼저 진단한다. 이 PC 전용 `localServiceId`를 항상 사용하고, exact VID/PID는 있으면 추가 AND 조건으로만 함께 기록하며 가명 ID를 외부에 공유하지 않는다. 표준 서비스가 없는 Bluetooth 기기만 제조사 전용 프로토콜 조사 대상으로 둔다.

지원 목록에 없으면 사용자가 프로토콜 자료를 제공하기를 기다리거나 장치 이름만 보고 `지원 보류(blocked)`로 끝내지 마라. 네트워크 조사 범위와 외부 전송 정보를 설명하고 내 승인을 받은 뒤 제조사 공식 문서와 지원/다운로드 페이지, 법적으로 검토 가능한 웹 드라이버 자산, 감사 가능한 기기별 오픈소스 구현을 직접 검색한다. URL, 버전·커밋 또는 확인 날짜, 정확한 대상 리비전과 라이선스를 기록하고, `--inventory`의 exact identity와 근거를 대응시켜 기존 프로필 추가인지 새 공급자 구현인지 판단한다. 조사 후에도 읽기 전용 프로토콜 근거가 부족한 경우에만 검색한 출처와 부족한 증거를 적어 지원 보류한다.

어떤 새 HID 명령도 보내지 말고 퍼징·명령 스캔·설정 변경도 하지 마라. 확인된 사실, 조사한 근거, 가능한 원인, 안전하게 구현 가능한 수정안과 남은 위험을 구분해서 보고하고 실제 수정은 내 승인을 기다려라.

진단할 기기(여러 개 가능, 관찰 증상은 기기마다 입력):
- 제품명/리비전: [값]
  연결 방식: [Bluetooth / 2.4GHz USB 동글 / 유선]
  관찰 증상: [표시 안 됨 / 퍼센트 오류 / 간헐적 끊김 등]
  마지막 정상 조건: [알고 있으면 입력]
- 제품명/리비전: [값]
  연결 방식: [값]
  관찰 증상: [값]
  마지막 정상 조건: [알고 있으면 입력]
```

## 3. 기본: 미지원 기기들을 LLM이 직접 조사·추가

사용자가 프로토콜 문서나 캡처를 미리 찾아야 하는 프롬프트가 아닙니다. 제품명과 연결 방식만 기기별로 알려도 에이전트가 읽기 전용 descriptor 인벤토리와 공개 자료 조사를 먼저 수행하고, 모든 기기를 같은 작업에서 각각 처리하며 근거에 따라 기존 공급자용 프로필 또는 별도 공급자를 선택합니다.

```text
이 Peripheral Battery Dashboard 저장소에 아래 목록의 모든 미지원 기기 호환성을 한 작업에서 추가해 줘. 각 기기의 exact identity와 프로토콜을 독립적으로 조사·검증하고 기기별 결과를 보고하라. 한 기기의 실패나 지원 보류 때문에 나머지를 생략하지 마라. 내가 프로토콜 문서나 명령 바이트를 먼저 찾아 주는 것을 전제로 하지 말고, 승인된 네트워크 접근과 redacted 읽기 전용 descriptor 인벤토리를 사용해 공개 근거를 네가 직접 조사해라.

모델 적합성: 표준 Bluetooth Battery Service 등록과 기존 공급자용 JSON 프로필 작성은 경량 모델로도 가능하다. 그러나 프로토콜이 알려지지 않은 USB 동글/HID의 프로토콜 추출과 새 공급자 구현은 frontier급 고수준 추론 에이전트가 필요하며, 현재 기준으로 GPT-5.6 Sol high/xhigh(특히 어려우면 max)를 권고한다. Luna급 모델은 이 작업의 단독 구현자나 단독 검토자가 되어서는 안 된다. 성공을 보장하는 정확한 최소 모델·추론 수준은 정할 수 없으므로 모델 이름 대신 원본 근거·fixture·빌드·self-test·실기기 검증·독립 검토로 완료를 판정한다. 현재 에이전트가 적합하지 않으면 안전한 인벤토리와 조사 기록을 고수준 에이전트에 인계하고 불완전한 지원을 설치하지 않는다.

`프로토콜이 저장소에 없다`거나 내가 프로토콜 자료를 주지 않았다는 이유로 중단하지 마라. 네트워크 조사 승인을 먼저 요청하고 제조사 자료·웹 드라이버 자산·감사 가능한 오픈소스·공개 raw source를 직접 검색하라. 문서의 검색 행렬을 실제로 수행한 기록 없이 `지원 보류(blocked)`를 선언하면 실패다. 사용자가 프로토콜을 찾아 오기를 기다리지 말고, 프로토콜 발견·근거 대조·안전한 구현 가능성 판정까지 네가 수행하라.

대상 저장소: https://github.com/rpr123/PeripheralBatteryDashboard

먼저 읽기 전용으로 Windows 10/11 x64 여부, .NET Framework 4.8, Git과 VS 2022 Build Tools 유무를 확인한다. 기존 로컬 저장소가 있으면 origin이 위 공식 URL과 같은지, HEAD와 dirty 상태를 확인하고 사용자 변경을 보존한다. 없으면 Git이 있을 때만 정확한 복제 위치와 네트워크·파일 변경을 설명하고 내 승인을 기다린 뒤 위 URL을 clone한다. 누락된 도구를 자동 설치하거나 권한을 상승하지 말고, 설치가 꼭 필요하면 정확한 출처·대상·영향을 설명해 별도 승인을 기다린다. origin이 다르면 그 복사본을 사용하지 않는다.

README.md, AGENTS.md, CODEX-PROMPTS.md, DEVICE-ADDING.md, Profiles/builtin.devices.json, Plugins/README.md, Plugins/SamplePlugin.cs.txt와 관련 공급자 코드를 읽고 다음 순서로 진행한다. `builtin.devices.json`의 빈 목록은 정상이며, 이를 지원 장치가 없다는 근거나 개인 장치를 기본값으로 추가할 이유로 사용하지 않는다.

1. 장치 I/O가 없는 --self-test를 실행한다. 이어서 대상·redacted 필드와 승인 경계를 설명하고, 승인 후 provider/battery 요청, HID input read·output write·Feature I/O와 GATT characteristic 값 읽기를 전혀 수행하지 않는 redacted `--inventory`로 Windows HID collection descriptor와 등록된 표준 Bluetooth Battery Service 인터페이스 메타데이터를 수집한다. 전체 장치 경로·시리얼·Bluetooth 주소·사용자명은 출력·파일·커밋에 남기지 않는다. XInput-only·audio-only·표준 Battery Service가 없는 Bluetooth 장치의 자동 발견은 보장하지 않는다.
   동시에 `bluetoothBatteryServices`에서 표준 BLE Battery Service 180F 메타데이터를 확인한다. 항목이 있으면 별도 제조사 공급자를 만들기 전에 `builtin.bluetooth.gatt-battery`와 `Transport=bluetooth-gatt`의 사용자 프로필로 처리한다. 이 PC 전용 `localServiceId`를 항상 사용하고, VID/PID는 있으면 추가 AND 조건으로만 함께 기록하며 가명 ID를 외부 검색·공개 프로필에는 넣지 않는다. 표준 서비스가 없는 Bluetooth 기기만 새 프로토콜 조사 대상으로 둔다.
2. 네트워크 조사 전에 검색 범위와 외부 전송 정보를 설명하고 내 승인을 기다린다. 승인되면 제조사 공식 제품·지원·다운로드 문서, 법적으로 검토 가능한 제조사 웹 드라이버 자산, 감사 가능한 기기별 오픈소스 구현 순으로 스스로 검색한다. 각 URL, 정확한 버전·커밋 또는 확인 날짜, 대상 하드웨어 리비전·연결 모드와 라이선스를 기록한다. 출처 불명 또는 라이선스가 없거나 맞지 않는 코드는 복사하지 않고, 인터넷에서 받은 실행 파일이나 DLL을 실행하지 않는다.
3. 제품명만 비슷하다는 이유로 호환성을 선언하지 말고 `--inventory`의 exact identity와 자료의 VID/PID·인터페이스·Usage·보고서 종류·바이트를 대응시킨다. 근거가 기존 공급자와 동일한 읽기 전용 요청/응답을 입증하면 좁게 매칭한 완전한 JSON 프로필을 만든다. 요청·응답·체크섬이 다르면 기존 공급자를 일반화하지 말고 별도 `IBatteryProvider` 플러그인과 프로필을 구현한다.
4. 확인된 모든 장치는 완전한 사용자 JSON에 등록한다. 공개 배포본의 `Profiles/builtin.devices.json`은 활성 프로필 0개를 유지하며 개인 장치를 추가하지 않는다. 전체 JSON, 적용 위치, 기존 파일 보존 방식과 변경 내용을 보여 주고 내 쓰기 승인을 받은 뒤 적용한다. 업스트림 변경이 필요하면 공급자·테스트·문서만 최소 범위로 수정하고 활성 기본 프로필은 만들지 않는다. 와일드카드나 느슨한 매칭으로 다른 장치까지 잡지 않는다.
5. 새 공급자는 정확한 보고서 타입·길이·고정 헤더·체크섬·0~100 범위, timeout·cancellation과 자원 해제를 검증한다. 프로필 fixture와 정상·오류·잘림 응답을 포함한 모의 응답 테스트, 격리된 Release 빌드와 --self-test를 실제 장치 요청 전에 통과시킨다.
   제공되거나 승인된 모든 정상 캡처와 문서화된 상태/subtype을 각각 fixture로 만들고 올바른 충전·연결 상태까지 검증한다. 일부 정상 표본을 무시하거나, 근거 없는 패딩·후행 0을 정상 조건으로 추가하면 완료가 아니다.
6. 알 수 없는 HID interrupt/output/Feature write, 무작위 바이트, 명령 공간 스캔·퍼징, 비슷한 다른 모델 명령의 추측 재사용, 펌웨어·페어링·DPI·키맵·조명·온보드 설정 변경은 절대 사용하지 않는다. 공개 자료를 실제로 검색했지만 exact hardware identity 또는 읽기 전용 배터리 프로토콜 근거가 부족한 경우에만 검색한 출처와 부족한 증거를 구체적으로 적어 `지원 보류(blocked)`로 판정한다. 사용자가 처음부터 프로토콜 자료를 주지 않았다는 사실만으로 보류하지 않는다.
7. --diagnostics와 --snapshot은 매칭된 공급자의 검증된 요청을 실제 장치에 보낼 수 있다. 새 명령의 첫 실기기 실행 전 정확한 VID/PID·MI·Usage Page/Usage, 보고서 타입과 길이, 전송 바이트, 읽기 전용 근거, 예상 응답과 실패 영향을 보여 주고 별도 승인을 기다린다. 승인 전에는 새 명령을 보내지 않는다.
8. 프로필이나 플러그인 적용 때문에 GUI를 실행하거나 재시작해야 하면 자동 실행 선택, 전체 settings 파일 또는 HKCU Run 변경, 모니터 시작과 exact-match 공급자의 실기기 I/O를 먼저 설명하고 별도 확인을 기다린다. 승인 없이는 GUI를 실행·재시작하지 않는다.
9. 조사한 출처·리비전·라이선스, 변경 파일, fixture·빌드·self-test 결과, 실기기 검증 여부와 남은 위험을 보고한다. 내 요청 없이 커밋·푸시·배포하지 않는다.

추가할 기기(한 줄에 하나씩, 여러 개 가능):
- [제품명/리비전] — [Bluetooth / 2.4GHz USB 동글 / 유선]
- [제품명/리비전] — [Bluetooth / 2.4GHz USB 동글 / 유선]
- ...

선택 메모:
- 제조사 프로그램 또는 문서: [알고 있으면 입력, 몰라도 됨]
```

## 4. 완전히 새로운 USB 동글 기기들 지원 검토

```text
이 Peripheral Battery Dashboard 저장소에서 아래 목록에 적은 모든 USB 동글/HID 기기의 배터리 지원을 각각 조사하고, exact hardware identity와 읽기 전용 프로토콜 근거가 충분한 기기는 같은 작업에서 별도 IBatteryProvider 플러그인으로 구현해 줘. 한 기기의 실패나 지원 보류 때문에 나머지를 생략하지 마라. 내가 프로토콜 문서나 명령 바이트를 먼저 제공하는 것을 전제로 하지 마라.

이 작업은 frontier급 고수준 추론 에이전트를 요구한다. 현재 기준으로 GPT-5.6 Sol high/xhigh를 기본 권고하고 특히 어려운 경우 max를 검토한다. Luna급 모델은 단독 구현자나 단독 검토자로 사용하지 않는다. 단, 장치·근거마다 난도가 달라 성공을 보장하는 정확한 최소 모델·추론 수준은 정할 수 없으므로 모델 이름만으로 완료를 선언하지 말고 원본 근거·모든 상태 fixture·빌드·self-test·실기기 검증·독립 검토를 통과해야 한다. 현재 에이전트가 적합하지 않으면 redacted 인벤토리와 조사 기록을 보존해 고수준 에이전트에 인계하고 불완전한 공급자를 설치하지 않는다.

저장소에 기존 프로토콜이 없거나 내가 캡처·명령 바이트를 제공하지 않았다는 이유로 즉시 중단하지 마라. 외부로 보낼 redacted 식별 정보와 검색 범위를 먼저 설명해 네트워크 승인을 받고, 승인 후 제조사 문서·지원/다운로드·검토 가능한 웹 드라이버·감사 가능한 기기별 오픈소스와 공개 raw source를 네가 직접 검색하라. 검색 행렬과 확인한 출처를 기록하지 않은 `지원 보류(blocked)`는 실패다.

대상 저장소: https://github.com/rpr123/PeripheralBatteryDashboard

먼저 읽기 전용으로 Windows 10/11 x64 여부, .NET Framework 4.8, Git과 VS 2022 Build Tools 유무를 확인한다. 기존 로컬 저장소가 있으면 origin이 위 공식 URL과 같은지, HEAD와 dirty 상태를 확인하고 사용자 변경을 보존한다. 없으면 Git이 있을 때만 정확한 복제 위치와 네트워크·파일 변경을 설명하고 내 승인을 기다린 뒤 위 URL을 clone한다. 누락된 도구를 자동 설치하거나 권한을 상승하지 말고, 설치가 꼭 필요하면 정확한 출처·대상·영향을 설명해 별도 승인을 기다린다. 승인받지 못했거나 현재 사용자 권한으로 안전하게 진행할 수 없으면 `지원 보류(blocked)`로 끝낸다. origin이 다르면 그 복사본을 사용하지 않는다.

작업 전에 README.md, AGENTS.md, CODEX-PROMPTS.md, DEVICE-ADDING.md, Plugins/README.md, Plugins/SamplePlugin.cs.txt와 기존 공급자 구조를 모두 읽어라.

1. 장치 I/O가 없는 --self-test와 현재 프로필/소스를 확인한다. 이어서 대상·redacted 필드와 승인 경계를 설명하고, 승인 후 provider/battery 요청과 HID input read·output write·Feature I/O를 전혀 수행하지 않는 redacted `--inventory`로 Windows HID collection의 VID/PID·MI·Usage Page/Usage·input/output/feature report 길이를 수집한다. 장치 시리얼, 전체 경로, 사용자명과 Bluetooth 주소는 출력·파일·커밋에서 제거한다. XInput-only·audio-only·비 HID Bluetooth 장치의 자동 발견은 보장하지 않는다. redacted --diagnostics와 --snapshot은 매칭된 기존 공급자의 검증된 상태 요청을 실제 장치에 보낼 수 있으므로 대상 ProviderId·매칭·수행될 I/O를 설명하고 내 명시적 확인을 받은 경우에만 사용한다.
2. 네트워크 조사 전에 검색 범위와 외부 전송 정보를 설명하고 내 승인을 기다린다. 승인되면 제조사 공식 제품 문서와 지원/다운로드 페이지, 법적으로 검토 가능한 제조사 웹 드라이버 자산, 감사 가능한 기기별 오픈소스 구현 순으로 읽기 전용 배터리 요청과 응답 형식을 스스로 찾는다. 각 URL, 정확한 버전·커밋 또는 확인 날짜, 대상 하드웨어 리비전·연결 모드와 라이선스를 기록한다. 출처 불명 또는 라이선스가 없거나 맞지 않는 코드는 복사하지 않고 인터넷에서 받은 실행 파일이나 DLL을 실행하지 않는다.
3. `--inventory`의 exact identity를 근거의 VID/PID·인터페이스·Usage·보고서 종류·바이트와 대응시킨다. 제품명, 브랜드 또는 비슷한 외형만으로 프로토콜을 재사용하지 않는다.
4. 공개 자료를 실제로 조사했는데도 exact hardware identity나 읽기 전용 배터리 프로토콜을 입증할 수 없다면 알 수 없는 HID interrupt/output/Feature write, 무작위 바이트, 명령 공간 스캔/퍼징 또는 다른 모델 명령의 추측 재사용을 절대 하지 않는다. 그때에만 검색한 출처, 확인한 사실, 부족한 증거와 안전한 후속 수집 방법을 구체적으로 적어 `지원 보류(blocked)`로 판정한다. 사용자가 자료를 미리 제공하지 않았다는 이유만으로 보류하지 않는다.
5. 근거가 충분하면 VID/PID·인터페이스·Usage를 좁게 제한한 독립 플러그인과 프로필을 만든다. 요청 길이와 타입, 응답 ID·헤더·길이·체크섬·0~100 범위, timeout과 cancellation, 자원 해제를 모두 검증한다. 기존 공급자를 임의로 일반화하지 않는다.
6. 정상 응답뿐 아니라 잘못된 ID·헤더·길이·체크섬·범위를 다루는 모의 응답 fixture 테스트, 프로필 fixture, 격리된 Release 빌드와 --self-test를 먼저 통과시킨다.
   제공되거나 승인된 모든 정상 캡처와 서로 다른 상태/subtype도 각각 이름 있는 fixture로 포함하고, 알 수 없는 subtype은 거부한다. 근거가 고정값이라고 입증하지 않은 패딩·후행 바이트를 0으로 강제하지 않는다.
7. 새 명령의 첫 실제 장치 실행 전에 정확한 VID/PID, 인터페이스(MI), Usage Page/Usage, 보고서 타입과 길이, 전송 바이트, 읽기 전용 근거, 예상 응답, 실패 시 영향과 복구 방법을 보여 주고 별도 승인을 받는다. 승인된 실제 판독만 한정적으로 수행하고 제조사 표시나 알려진 배터리 상태와 비교한다. 펌웨어·페어링·DPI·키맵·조명·온보드 설정을 바꾸는 명령은 사용하지 않는다.
8. 플러그인 적용 때문에 GUI를 실행하거나 재시작해야 하면 자동 실행 선택, 전체 settings 파일 또는 HKCU Run 변경, 모니터 시작과 새 exact-match 공급자의 실기기 I/O를 먼저 설명하고 별도 확인을 기다린다. 승인 없이는 GUI를 실행·재시작하지 않고 적용 검증을 보류한다.
9. 변경 파일, 조사한 URL·리비전·라이선스, 테스트 결과, 실제 검증 범위와 남은 위험을 보고한다. 내 요청 없이 DLL을 인터넷에서 내려받아 실행하거나 커밋·푸시·릴리스하지 않는다.

검토할 기기(한 줄에 하나씩, 여러 개 가능):
- [제품명/정확한 리비전] — [2.4GHz USB 동글 / 기타 HID 연결]
- [제품명/정확한 리비전] — [2.4GHz USB 동글 / 기타 HID 연결]
- ...

선택 메모:
- 제조사 소프트웨어/문서: [알고 있으면 입력, 몰라도 됨]
- 제공 가능한 redacted diagnostics 또는 캡처: [있으면 입력, 없어도 됨]
```

## 5. 설치본 제거

```text
이 Windows PC에서 Peripheral Battery Dashboard를 안전하게 제거해 줘. 사용자가 폴더나 레지스트리를 직접 지우도록 넘기지 마라.

먼저 읽기 전용으로 실행 중인 Peripheral Battery Dashboard 프로세스의 정확한 실행 경로, 실제 설치 폴더, `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\PeripheralBatteryDashboard` 값, `%LOCALAPPDATA%\PeripheralBatteryDashboard`의 설정·배터리 기록·사용자 프로필 존재 여부를 확인한다. 다른 프로세스나 이름이 비슷한 폴더는 대상으로 삼지 마라.

앱 종료, 자동 실행 값 제거, 설치 폴더 제거, 사용자 설정·배터리 기록·장치 프로필의 보존 또는 삭제를 각각 구분해 정확한 대상과 영향을 보여 줘. 특히 사용자 프로필은 재설치 때 재사용할 수 있으므로 보존할지 삭제할지 묻고, 내 승인을 받은 항목만 변경하라.

승인 후 정확한 앱 프로세스를 종료하고 자동 실행 값을 제거한 다음, 승인된 설치 폴더와 사용자 데이터만 제거하라. 완료 후 실행 중인 앱이 없는지, 자동 실행 값이 남지 않았는지, 보존하거나 삭제한 사용자 데이터와 제거하지 않은 항목을 보고하라. 내 요청 없이 저장소, 빌드 도구, 드라이버 또는 관련 없는 파일을 삭제하지 마라.
```

## 에이전트 결과를 확인할 때

다음 조건을 충족하지 않으면 실제 장치에서 새 플러그인을 실행하지 마세요.

- 정확한 하드웨어 매칭과 읽기 전용 요청의 출처가 설명되어 있습니다.
- 에이전트가 사용자의 자료를 기다리지 않고 공식 문서·웹 드라이버·감사 가능한 오픈소스를 조사했으며 URL·정확한 리비전·라이선스를 기록했습니다.
- 명령 추측, 퍼징 또는 와일드카드 매칭을 사용하지 않았습니다.
- 빌드와 `--self-test` 결과가 포함되어 있습니다.
- 모의 응답 검증과 오류 처리 범위가 설명되어 있습니다.
- 진단·diff·커밋에 개인 식별 정보가 없습니다.
- 사용자가 승인하지 않은 자동 실행, 외부 프로그램 설치 또는 게시가 없습니다.

Codex가 공개 자료 조사 없이 곧바로 지원 보류를 선택하면 프롬프트를 다시 지시하는 데서 그치지 말고, 신규 동글 작업이라면 frontier급 고수준 추론 에이전트로 같은 문서와 redacted `--inventory`를 인계하세요. 현재 기준 권고는 GPT-5.6 Sol high/xhigh이지만, 정확한 최소 등급은 장치마다 달라 보장할 수 없습니다. 충분히 조사한 뒤에도 근거가 불충분한 경우에는 보류가 올바르며, 모델을 바꾸더라도 안전 규칙과 독립 검토 기준은 그대로 유지합니다.
